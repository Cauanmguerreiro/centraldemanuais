/**
 * Placeholder de banco de dados do AgaClube Help.
 *
 * Este arquivo não é importado pelo site nesta fase.
 * Não adicione credenciais reais ao repositório.
 */
export const databaseConfig = Object.freeze({
  provider: "placeholder",
  apiUrl: "",
  projectId: "",
  publicKey: ""
});
